/**
 * Os ícones vivem em `@campanha/icons` porque o painel precisa renderizar os
 * mesmos SVGs no seletor de ícone das propostas. Este arquivo permanece como
 * ponto de entrada para que nenhum import do site precise mudar.
 */
export * from "@campanha/icons";
